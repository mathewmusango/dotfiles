# Contributing

This repository is my machine configuration, installed by copy — it runs no service.

It is maintained by one person and does not take outside contributions — changes are made through a pull request against `main`, where the shared checks run and the merge is squashed.
