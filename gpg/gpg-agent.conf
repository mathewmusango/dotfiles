# GnuPG agent — passphrase dialog and caching.
# pinentry-timeout is load-bearing: the built-in ~60 s default expires before the
# dialog is answered, which fails key generation / signing with `Timeout`.
pinentry-program /usr/bin/pinentry-qt
pinentry-timeout 600
default-cache-ttl 28800
max-cache-ttl 86400
